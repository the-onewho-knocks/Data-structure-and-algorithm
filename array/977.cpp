#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> nums = {-4, -1, 0, 3, 10};
    vector<int> v(nums);
    vector<int> n;
    int left = 0;
    int right = 1;
    int index = nums.size();
}